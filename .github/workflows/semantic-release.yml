name: Semantic Release

on:
  push:
    branches:
      - main

permissions:
  contents: write
  issues: write
  pull-requests: write

jobs:
  release:
    name: Semantic Release
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run semantic-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: npx semantic-release

      - name: Check if new version was released
        id: check_release
        run: |
          if git describe --exact-match --tags HEAD 2>/dev/null; then
            echo "NEW_RELEASE=true" >> $GITHUB_OUTPUT
            echo "VERSION=$(git describe --exact-match --tags HEAD | sed 's/^v//')" >> $GITHUB_OUTPUT
          else
            echo "NEW_RELEASE=false" >> $GITHUB_OUTPUT
          fi

      - name: Output release information
        if: steps.check_release.outputs.NEW_RELEASE == 'true'
        run: |
          echo "🎉 New version released: ${{ steps.check_release.outputs.VERSION }}"
          echo "📦 GitHub Release created with tag v${{ steps.check_release.outputs.VERSION }}"
          echo "🚀 Release workflow will now build and publish to Chrome Web Store"
